# 小红书博主笔记提取脚本 - 环境配置文件
# 请复制此文件为 .env 并填入真实的配置信息

# ========================================
# 必需配置
# ========================================

# 小红书 Cookie (必需)
# 获取方法见 COOKIE_GUIDE.md
XHS_COOKIE=

# ========================================
# 可选配置
# ========================================

# User-Agent (可选,建议使用真实浏览器的 UA)
# 示例: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
XHS_USER_AGENT=

# 代理服务器 (可选)
# 格式: http://127.0.0.1:7890 或 socks5://127.0.0.1:1080
XHS_PROXY=

# 请求延迟时间范围 (秒, 可选, 默认 2-5)
# 格式: 最小值-最大值
XHS_DELAY_RANGE=2-5

# 最大重试次数 (可选, 默认 3)
XHS_MAX_RETRY=3

# 请求超时时间 (秒, 可选, 默认 10)
XHS_TIMEOUT=10

# 数据保存目录 (可选, 默认 downloads)
XHS_DOWNLOAD_DIR=downloads

# 是否保存为 CSV 格式 (可选, 默认 true)
XHS_SAVE_CSV=true

# 是否保存为 JSON 格式 (可选, 默认 true)
XHS_SAVE_JSON=true

# 是否下载媒体文件(视频/图片) (可选, 默认 true)
# 设置为 true 会自动下载笔记中的所有图片和视频
# 媒体文件会保存在 downloads/作者ID_作者昵称/笔记文件夹/ 中
XHS_DOWNLOAD_MEDIA=true
